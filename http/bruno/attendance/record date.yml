info:
  name: record date
  type: http
  seq: 2

http:
  method: PUT
  url: https://{{ApiHost}}/api/attendance/:date
  params:
    - name: date
      value: 2026-09-25
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
